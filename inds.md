# Документация по настройке Asterisk для работы с GSM-шлюзами Grandstream

## 🎯 Цель
Правки для корректной работы входящих звонков через GSM-шлюзы Grandstream GXW4108 на хосте **0307-inds** (10.88.10.61:5059).

## ❌ Проблема
Без этих правок звонки обрываются через 5-10 секунд после установления соединения из-за:
1. Преждевременной отправки `200 OK` (до проигрывания приветствия)
2. GSM-шлюз не отправляет `ACK` в ответ на `200 OK`
3. Asterisk считает GSM-шлюз внешним устройством (NAT)
4. Session timers вызывают re-INVITE, на который шлюз не отвечает

---

## 📝 ПРАВКИ

### 1. Файл `/etc/asterisk/sip.conf` - Добавить локальную сеть GSM-шлюза

**Найти секцию с `localnet` и добавить:**

```ini
localnet=192.168.1.0/24
localnet=192.168.100.0/24    ← ДОБАВИТЬ ЭТУ СТРОКУ!
```

**Почему:** Asterisk должен знать, что `192.168.100.154` (адрес GSM-шлюза) - это локальная сеть, чтобы в поле `Contact` использовать локальный IP, а не внешний `178.124.218.76`.

---

### 2. Файл `/etc/asterisk/extensions.conf` - Исправить контекст `[from-out-office]`

**Найти:**
```asterisk
[from-out-office]
exten => _X.,1,Set(AUDIOHOOK_INHERIT(MixMonitor)=yes)
same => n,Set(Trunk=${EXTEN})
same => n,Answer                    ← ЗАКОММЕНТИРОВАТЬ!
same => n,Macro(incall_start,${Trunk})
same => n,Set(CALLERID(num)=${CALLERID(name)})
same => n,Answer                    ← ЗАКОММЕНТИРОВАТЬ!
exten => _X.,n,MixMonitor(${UNIQUEID}.wav)
```

**Заменить на:**
```asterisk
[from-out-office]
exten => _X.,1,Set(AUDIOHOOK_INHERIT(MixMonitor)=yes)
same => n,Set(Trunk=${EXTEN})
;same => n,Answer                   ← ЗАКОММЕНТИРОВАТЬ!
same => n,Macro(incall_start,${Trunk})
same => n,Set(CALLERID(num)=${CALLERID(name)})
;same => n,Answer                   ← ЗАКОММЕНТИРОВАТЬ!
exten => _X.,n,MixMonitor(${UNIQUEID}.wav)
```

**Почему:** `Answer()` вызывался слишком рано и отправлял `200 OK` до того, как звонок перешел в контекст обработки, где должен был сначала отправиться `183 Session Progress`.

---

### 3. Файл `/etc/asterisk/extensions.conf` - Исправить контекст обработки входящих

**Найти контекст типа `[d86372e0]` или `[16acd97e]` (имя может отличаться):**

```asterisk
[d86372e0]
exten => _X.,1,Noop
same => n,Playback(custom/f286db5f)    ← Или может не быть Playback
same => n,Macro(incall_dial,${Trunk},155)
```

**Заменить на:**
```asterisk
[d86372e0]
exten => _X.,1,Noop
same => n,Progress()                    ← ДОБАВИТЬ! (183 Session Progress)
same => n,Wait(1)                       ← ДОБАВИТЬ! (пауза 1 сек)
same => n,Answer()                      ← ДОБАВИТЬ! (только теперь 200 OK)
same => n,Playback(custom/f286db5f)     ← Если было
same => n,Macro(incall_dial,${Trunk},155)
```

**Или если Playback нет:**
```asterisk
[d86372e0]
exten => _X.,1,Noop
same => n,Progress()                    ← ДОБАВИТЬ!
same => n,Wait(1)                       ← ДОБАВИТЬ!
same => n,Answer()                      ← ДОБАВИТЬ!
same => n,Macro(incall_dial,${Trunk},155)
```

**Почему:**
- `Progress()` - отправляет `183 Session Progress` вместо `200 OK`
- `Wait(1)` - небольшая пауза для стабилизации
- `Answer()` - только после этого отправляется `200 OK`, и GSM-шлюз корректно отправляет `ACK`

---

### 4. Файл `/etc/asterisk/sip_addproviders.conf` - Настройки для линий GSM-шлюза

**Для каждой линии, подключенной к GSM-шлюзу (0001364, 0001365, и т.д.):**

**Было:**
```ini
[0001364]
host=dynamic
type=peer
secret=4bfX5XuefNp3aksfhj232
callgroup=1
pickupgroup=1
disallow=all
allow=ulaw
context=from-out-office
directmedia=no
nat=force_rport,comedia
qualify=8000
insecure=invite
defaultuser=s
```

**Добавить в конец секции:**
```ini
[0001364]
host=dynamic
type=peer
secret=4bfX5XuefNp3aksfhj232
callgroup=1
pickupgroup=1
disallow=all
allow=ulaw
context=from-out-office
directmedia=no
nat=force_rport,comedia
qualify=8000
insecure=invite
defaultuser=s
progressinband=yes          ← ДОБАВИТЬ
sendrpid=yes               ← ДОБАВИТЬ
trustrpid=yes              ← ДОБАВИТЬ
timert1=1000               ← ДОБАВИТЬ
timerb=64000               ← ДОБАВИТЬ
session-timers=refuse      ← ДОБАВИТЬ
session-minse=90           ← ДОБАВИТЬ
session-expires=7200       ← ДОБАВИТЬ
session-refresher=uac      ← ДОБАВИТЬ
autoframing=yes            ← ДОБАВИТЬ
rewrite_contact=yes        ← ДОБАВИТЬ
```

**Назначение параметров:**

| Параметр | Значение | Зачем |
|----------|----------|-------|
| `progressinband=yes` | Разрешить early media | Позволяет проигрывать аудио до Answer() через 183 |
| `sendrpid=yes` | Отправлять Remote Party ID | Для передачи информации о вызывающем |
| `trustrpid=yes` | Доверять RPID от провайдера | Принимать CallerID |
| `timert1=1000` | Базовый таймер 1 сек | Увеличен с 500ms для медленных GSM-шлюзов |
| `timerb=64000` | Таймаут INVITE 64 сек | Увеличен с 32 сек для медленных GSM-шлюзов |
| `session-timers=refuse` | Отключить session timers | GSM-шлюзы не умеют обрабатывать re-INVITE |
| `session-minse=90` | Минимум 90 сек | Минимальный интервал обновления сессии |
| `session-expires=7200` | Истечение через 2 часа | Увеличен с 30 минут (1800 сек) |
| `session-refresher=uac` | Клиент обновляет | Если таймеры всё же используются, шлюз обновляет (не Asterisk) |
| `autoframing=yes` | Автофрейминг | Автоматическая подстройка под GSM-шлюз |
| `rewrite_contact=yes` | Переписывать Contact | Использовать локальный IP в Contact вместо внешнего |

---

## 🔄 Применение изменений

После внесения правок выполнить:

```bash
asterisk -rx "sip reload"
asterisk -rx "dialplan reload"
```

Или:
```bash
asterisk -rx "core reload"
```

---

## ✅ Проверка работы

### 1. Проверить статус линии:
```bash
asterisk -rx "sip show peer 0001364"
```

Должно быть:
- `Status: OK (XX ms)`
- `Addr->IP: 192.168.100.154:XXXX`
- `Sess-Timers: Refuse`
- `Auto-Framing: Yes`

### 2. Проверить диалплан:
```bash
asterisk -rx "dialplan show d86372e0"
```

Должна быть последовательность:
```
1. Noop()
2. Progress()
3. Wait(1)
4. Answer()
5. ...
```

### 3. Тестовый звонок:
- Позвонить на линию GSM-шлюза
- Должно быть 5-7 гудков
- Поднять трубку
- Разговор должен продолжаться без обрывов

### 4. При проблемах - включить SIP debug:
```bash
asterisk -rx "sip set debug peer 0001364"
# Сделать тестовый звонок
asterisk -rx "sip set debug off"
```

---

## 🔍 Диагностика проблем

### Проблема: Звонок обрывается через 5-10 секунд

**Причина:** GSM-шлюз не отправляет ACK на `200 OK`

**Решение:**
1. Проверить `localnet=192.168.100.0/24` в `/etc/asterisk/sip.conf`
2. Проверить `rewrite_contact=yes` в конфиге линии
3. В SIP debug смотреть, какой IP в поле `Contact` в `200 OK` (должен быть `192.168.100.170`, а не `178.124.218.76`)

### Проблема: Звонок обрывается сразу после установления

**Причина:** Преждевременный `Answer()` в `[from-out-office]`

**Решение:**
1. Закомментировать оба `Answer()` в `[from-out-office]`
2. Добавить `Progress()`, `Wait(1)`, `Answer()` в контекст обработки

### Проблема: Нет звука в одну или обе стороны

**Причина:** Проблемы с RTP/NAT

**Решение:**
1. Проверить `nat=force_rport,comedia` в конфиге линии
2. Проверить `directmedia=no` в конфиге линии
3. Проверить фаервол

---

## 📊 Информация о хосте

- **Hostname:** 0307-inds
- **IP:** 10.88.10.61
- **SSH Port:** 5059
- **Asterisk:** 12.8.2
- **GSM-шлюз:** Grandstream GXW4108 (HW 2.0) v1.3.4.13
- **GSM-шлюз IP:** 192.168.100.154
- **Asterisk Internal IP:** 192.168.100.170
- **Asterisk External IP:** 178.124.218.76:50625

---

## 📝 История изменений

**04.11.2025:**
- Исправлена проблема с обрывом звонков через 5-10 секунд
- Добавлена поддержка GSM-шлюза Grandstream GXW4108
- Настроены линии 0001364 и 0001365
- Добавлены параметры session timers и NAT

---

## ⚠️ ВАЖНО!

После каждой автогенерации файлов `extensions.conf` и `sip_addproviders.conf` **ОБЯЗАТЕЛЬНО** применять эти правки вручную или автоматически (через скрипт).

Без этих правок звонки через GSM-шлюзы **НЕ БУДУТ РАБОТАТЬ!**


