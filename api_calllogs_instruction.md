# 📞 Инструкция по работе с API получения звонков

## 📋 Общая информация

**Адрес API:** `https://crm.vochi.by/api/calllogs`  
**Метод:** `GET`  
**Формат ответа:** JSON (массив объектов)

---

## 🔧 Параметры запроса

| Параметр | Обязательный | Формат | Описание | Пример |
|----------|--------------|--------|----------|--------|
| `start` | ✅ Да | `YYYY-MM-DD` | Дата начала периода | `2025-11-10` |
| `end` | ✅ Да | `YYYY-MM-DD` | Дата окончания периода | `2025-11-10` |
| `clientId` | ✅ Да | `строка (UUID)` | Идентификатор клиента | `7e578de4eddc403cbc49260fcc76bbe9` |

### ⚠️ ВАЖНО: Формат даты

**Используйте ТОЛЬКО формат `YYYY-MM-DD` (ISO 8601):**
- ✅ **ПРАВИЛЬНО:** `2025-11-10` (10 ноября 2025)
- ❌ **НЕПРАВИЛЬНО:** `10-11-2025` (будет интерпретировано как 11 октября!)
- ❌ **НЕПРАВИЛЬНО:** `10/11/2025` (будет интерпретировано как 11 октября!)

---

## 📊 Структура ответа

API возвращает массив объектов со следующими полями:

```json
[
  {
    "CallLogId": "691188737867b5c3e18a2285",
    "Start": "2025-11-10 09:38:43",
    "End": "2025-11-10 09:39:40",
    "Duration": 57,
    "Destination": "151",
    "CallerId": "375296920799",
    "UniqueId": "1762756723.0",
    "CallStatusId": 2,
    "CallType": 0,
    "TrunkName": "375256813344"
  }
]
```

### Описание полей:

- **`CallLogId`** — уникальный идентификатор записи звонка
- **`Start`** — время начала звонка (формат: `YYYY-MM-DD HH:MM:SS`)
- **`End`** — время окончания звонка (формат: `YYYY-MM-DD HH:MM:SS`)
- **`Duration`** — длительность звонка в секундах
- **`Destination`** — номер назначения (куда звонили)
- **`CallerId`** — номер звонящего (кто звонил)
- **`UniqueId`** — уникальный идентификатор звонка в Asterisk
- **`CallStatusId`** — статус звонка (см. таблицу ниже)
- **`CallType`** — тип звонка (см. таблицу ниже)
- **`TrunkName`** — имя SIP-линии

### 📌 Значения `CallStatusId`:

| CallStatusId | Описание | Частота |
|--------------|----------|---------|
| `1` | Специальный статус | ~0.03% |
| `2` | ✅ Обычный завершенный звонок | ~99.5% |
| `5` | ⚠️ Некорректный/зависший звонок | ~0.4% |

### 📌 Значения `CallType`:

| CallType | Описание |
|----------|----------|
| `0` | 📥 **Входящий** звонок |
| `1` | 📤 **Исходящий** звонок |
| `2` | 🔄 **Внутренний** звонок (между сотрудниками) |

---

## 🚀 Примеры использования

### 1️⃣ Получить ВСЕ звонки за день

**Запрос:**
```bash
curl "https://crm.vochi.by/api/calllogs?start=2025-11-10&end=2025-11-10&clientId=7e578de4eddc403cbc49260fcc76bbe9"
```

**Пример на Python:**
```python
import requests
import json

url = "https://crm.vochi.by/api/calllogs"
params = {
    "start": "2025-11-10",
    "end": "2025-11-10",
    "clientId": "7e578de4eddc403cbc49260fcc76bbe9"
}

response = requests.get(url, params=params)
calls = response.json()

print(f"Всего звонков: {len(calls)}")
for call in calls[:5]:  # Первые 5 записей
    print(f"{call['Start']} | {call['CallerId']} → {call['Destination']} | {call['Duration']}s")
```

**Пример на PHP:**
```php
<?php
$url = "https://crm.vochi.by/api/calllogs";
$params = [
    'start' => '2025-11-10',
    'end' => '2025-11-10',
    'clientId' => '7e578de4eddc403cbc49260fcc76bbe9'
];

$response = file_get_contents($url . '?' . http_build_query($params));
$calls = json_decode($response, true);

echo "Всего звонков: " . count($calls) . "\n";
foreach ($calls as $call) {
    echo "{$call['Start']} | {$call['CallerId']} → {$call['Destination']} | {$call['Duration']}s\n";
}
?>
```

**Пример на JavaScript:**
```javascript
const url = "https://crm.vochi.by/api/calllogs";
const params = new URLSearchParams({
    start: "2025-11-10",
    end: "2025-11-10",
    clientId: "7e578de4eddc403cbc49260fcc76bbe9"
});

fetch(`${url}?${params}`)
    .then(response => response.json())
    .then(calls => {
        console.log(`Всего звонков: ${calls.length}`);
        calls.forEach(call => {
            console.log(`${call.Start} | ${call.CallerId} → ${call.Destination} | ${call.Duration}s`);
        });
    });
```

---

### 2️⃣ Получить только ВХОДЯЩИЕ звонки

API не поддерживает фильтрацию на сервере, поэтому фильтруйте данные после получения:

**Python:**
```python
import requests

url = "https://crm.vochi.by/api/calllogs"
params = {
    "start": "2025-11-10",
    "end": "2025-11-10",
    "clientId": "7e578de4eddc403cbc49260fcc76bbe9"
}

response = requests.get(url, params=params)
all_calls = response.json()

# Фильтруем только входящие (CallType = 0)
incoming_calls = [call for call in all_calls if call['CallType'] == 0]

print(f"Всего звонков: {len(all_calls)}")
print(f"Входящих звонков: {len(incoming_calls)}")

for call in incoming_calls[:10]:  # Первые 10
    print(f"{call['Start']} | От: {call['CallerId']} | Длительность: {call['Duration']}s")
```

**PHP:**
```php
<?php
$url = "https://crm.vochi.by/api/calllogs";
$params = [
    'start' => '2025-11-10',
    'end' => '2025-11-10',
    'clientId' => '7e578de4eddc403cbc49260fcc76bbe9'
];

$response = file_get_contents($url . '?' . http_build_query($params));
$all_calls = json_decode($response, true);

// Фильтруем только входящие (CallType = 0)
$incoming_calls = array_filter($all_calls, function($call) {
    return $call['CallType'] === 0;
});

echo "Всего звонков: " . count($all_calls) . "\n";
echo "Входящих звонков: " . count($incoming_calls) . "\n";
?>
```

---

### 3️⃣ Получить только ИСХОДЯЩИЕ звонки

**Python:**
```python
import requests

url = "https://crm.vochi.by/api/calllogs"
params = {
    "start": "2025-11-10",
    "end": "2025-11-10",
    "clientId": "7e578de4eddc403cbc49260fcc76bbe9"
}

response = requests.get(url, params=params)
all_calls = response.json()

# Фильтруем только исходящие (CallType = 1)
outgoing_calls = [call for call in all_calls if call['CallType'] == 1]

print(f"Исходящих звонков: {len(outgoing_calls)}")

for call in outgoing_calls[:10]:
    print(f"{call['Start']} | Кому: {call['Destination']} | Длительность: {call['Duration']}s")
```

---

### 4️⃣ Получить звонки короче N секунд (потенциально пропущенные)

**Python:**
```python
import requests

url = "https://crm.vochi.by/api/calllogs"
params = {
    "start": "2025-11-10",
    "end": "2025-11-10",
    "clientId": "7e578de4eddc403cbc49260fcc76bbe9"
}

response = requests.get(url, params=params)
all_calls = response.json()

# Фильтруем звонки короче 5 секунд (возможно пропущенные)
short_calls = [call for call in all_calls if call['Duration'] < 5]

print(f"Звонков короче 5 секунд: {len(short_calls)}")

for call in short_calls:
    call_type = "Входящий" if call['CallType'] == 0 else "Исходящий"
    print(f"{call['Start']} | {call_type} | {call['CallerId']} → {call['Destination']} | {call['Duration']}s")
```

---

### 5️⃣ Получить звонки за период (диапазон дат)

**Python:**
```python
import requests
from datetime import datetime

url = "https://crm.vochi.by/api/calllogs"
params = {
    "start": "2025-11-01",  # Начало периода
    "end": "2025-11-10",    # Конец периода
    "clientId": "7e578de4eddc403cbc49260fcc76bbe9"
}

response = requests.get(url, params=params)
calls = response.json()

print(f"Звонков за период: {len(calls)}")

# Группируем по дням
from collections import defaultdict
calls_by_day = defaultdict(int)

for call in calls:
    day = call['Start'].split(' ')[0]  # Получаем только дату
    calls_by_day[day] += 1

print("\nРаспределение по дням:")
for day in sorted(calls_by_day.keys()):
    print(f"{day}: {calls_by_day[day]} звонков")
```

---

### 6️⃣ Получить только ПРОПУЩЕННЫЕ входящие звонки

**⚠️ ВНИМАНИЕ:** API не имеет специального поля для пропущенных звонков.  
Используйте эвристику: входящие звонки с Duration < 5 секунд (не успели ответить).

**Python:**
```python
import requests

url = "https://crm.vochi.by/api/calllogs"
params = {
    "start": "2025-11-10",
    "end": "2025-11-10",
    "clientId": "7e578de4eddc403cbc49260fcc76bbe9"
}

response = requests.get(url, params=params)
all_calls = response.json()

# Фильтруем:
# 1. Входящие (CallType = 0)
# 2. Короткие (Duration < 5 секунд)
missed_calls = [
    call for call in all_calls 
    if call['CallType'] == 0 and call['Duration'] < 5
]

print(f"Потенциально пропущенных звонков: {len(missed_calls)}")

for call in missed_calls:
    print(f"{call['Start']} | От: {call['CallerId']} | Длительность: {call['Duration']}s")
```

---

### 7️⃣ Статистика по звонкам

**Python:**
```python
import requests
from collections import Counter

url = "https://crm.vochi.by/api/calllogs"
params = {
    "start": "2025-11-10",
    "end": "2025-11-10",
    "clientId": "7e578de4eddc403cbc49260fcc76bbe9"
}

response = requests.get(url, params=params)
calls = response.json()

# Статистика по типам
call_types = Counter([call['CallType'] for call in calls])
type_names = {0: "Входящие", 1: "Исходящие", 2: "Внутренние"}

print(f"=== СТАТИСТИКА за {params['start']} ===\n")
print(f"Всего звонков: {len(calls)}\n")

print("По типам:")
for call_type, count in sorted(call_types.items()):
    print(f"  {type_names.get(call_type, f'Тип {call_type}')}: {count}")

# Статистика по длительности
durations = [call['Duration'] for call in calls]
total_duration = sum(durations)
avg_duration = total_duration / len(durations) if durations else 0

print(f"\nДлительность:")
print(f"  Общая: {total_duration}s ({total_duration // 60} мин)")
print(f"  Средняя: {avg_duration:.1f}s")
print(f"  Самый короткий: {min(durations)}s")
print(f"  Самый длинный: {max(durations)}s")
```

**Пример вывода:**
```
=== СТАТИСТИКА за 2025-11-10 ===

Всего звонков: 151

По типам:
  Входящие: 92
  Исходящие: 58
  Внутренние: 1

Длительность:
  Общая: 16234s (270 мин)
  Средняя: 107.5s
  Самый короткий: 7s
  Самый длинный: 725s
```

---

## 🔍 Расшифровка значений

### CallType (Тип звонка)
- `0` = 📥 **Входящий** — звонок пришел извне на вашу компанию
- `1` = 📤 **Исходящий** — звонок совершен вашим сотрудником
- `2` = 🔄 **Внутренний** — звонок между сотрудниками внутри компании

### CallStatusId (Статус звонка)
- `1` = Специальный статус (редкий случай)
- `2` = ✅ **Нормальный завершенный звонок** (99.5% всех звонков)
- `5` = ⚠️ Некорректный/зависший звонок (обычно игнорируйте)

---

## ❓ FAQ

### 1. Как получить только пропущенные звонки?

API не предоставляет прямого признака "пропущен/отвечен". Используйте эвристику:
```python
# Входящие звонки с Duration < 5 секунд считаются пропущенными
missed = [c for c in calls if c['CallType'] == 0 and c['Duration'] < 5]
```

### 2. Почему Duration может быть 0?

Это звонки, которые были сброшены мгновенно или не установились.

### 3. Что означает Duration > 1000000 секунд?

Это CallStatusId=5 — некорректные/зависшие звонки. Фильтруйте их:
```python
normal_calls = [c for c in calls if c['CallStatusId'] == 2]
```

### 4. Можно ли получить звонки за несколько месяцев?

Да, но учитывайте, что API может возвращать много данных. Рекомендуется:
- Делать запросы по неделям/месяцам
- Использовать пагинацию на своей стороне

### 5. Есть ли лимит на количество возвращаемых записей?

API возвращает все записи за указанный период. Будьте осторожны с широкими диапазонами.

---

## 📞 Техническая поддержка

Если возникли вопросы или проблемы с API:
1. Проверьте формат даты (`YYYY-MM-DD`)
2. Убедитесь, что `clientId` корректен
3. Проверьте, что период не слишком большой
4. Обратитесь в службу поддержки с примером запроса и ошибкой

---

**Дата создания:** 2025-11-11  
**Версия:** 1.0


